/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['encrypted-tbn0.gstatic.com', 'theedunepal.ap-south-1.linodeobjects.com'],
  },
}

module.exports = nextConfig